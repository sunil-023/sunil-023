#include<stdio.h>
main()
{
	int a,b;
	printf("the maximum of two numbers");
	scanf("%d",&a,&b);
	if (a>b)
	{
	printf("the maximun of two numbers%d",a);	
	}
	else
	{
		printf("the maximum of two numbers%d",a);
	}
}
